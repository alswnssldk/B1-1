# Linux Agent 운영 환경 구축 및 관제 자동화 수행내역서

> 이 문서는 실제 실행 증거를 채우기 위한 제출용 템플릿입니다. `[실행 결과 붙여넣기]`를 본인의 실제 명령 결과 또는 스크린샷 참조로 교체하십시오.

## 1. 수행 환경

| 항목 | 내용 |
|---|---|
| OS | Ubuntu 22.04 LTS 컨테이너 |
| 실행 방식 | Docker Compose |
| 컨테이너 이름 | `agent-linux` |
| SSH 포트 | `20022/tcp` |
| 앱 포트 | `15034/tcp` |
| AGENT_HOME | `/home/agent-admin/agent-app` |
| 로그 경로 | `/var/log/agent-app` |

### 실행 환경 확인

```bash
cat /etc/os-release
uname -m
docker compose ps
```

```text
[실행 결과 붙여넣기]
```

## 2. SSH 보안 설정

### 적용 설정

```text
Port 20022
PermitRootLogin no
PasswordAuthentication yes
```

### 확인 명령

```bash
cat /etc/ssh/sshd_config.d/99-agent-mission.conf
sshd -T | grep -E '^(port|permitrootlogin|passwordauthentication) '
ss -ltnp | grep ':20022'
```

### 실행 결과

```text
[실행 결과 붙여넣기]
```

### 적용 이유

기본 포트 변경은 보안 통제의 전부는 아니지만, 기본 22번 포트를 대상으로 반복되는 자동 스캔과 무차별 대입 시도를 줄이는 보조 통제다. Root 원격 로그인을 차단하면 공격자가 최고 권한 계정으로 직접 인증하는 경로를 제거할 수 있다. 운영자는 일반 계정으로 로그인한 뒤 필요한 작업만 `sudo`로 수행하므로 최소 권한 적용과 사용자별 행위 추적에도 유리하다.

## 3. UFW 방화벽

### 정책

- 기본 인바운드: 차단
- 기본 아웃바운드: 허용
- 허용 인바운드: `20022/tcp`, `15034/tcp`

### 확인 명령

```bash
ufw status verbose
ufw status numbered
```

### 실행 결과

```text
[실행 결과 붙여넣기]
```

### 적용 이유

서비스 제공에 필요한 포트만 열어 공격 표면을 줄였다. SSH 관리용 20022와 앱 서비스용 15034 이외의 신규 인바운드 연결은 기본적으로 차단된다.

## 4. 계정과 그룹

| 계정 | 역할 | 그룹 |
|---|---|---|
| `agent-admin` | 운영·관리, cron 실행 | `agent-common`, `agent-core` |
| `agent-dev` | 개발·운영, monitor.sh 소유 | `agent-common`, `agent-core` |
| `agent-test` | QA·업로드 테스트 | `agent-common` |

### 확인 명령

```bash
id agent-admin
id agent-dev
id agent-test
getent group agent-common
getent group agent-core
```

### 실행 결과

```text
[실행 결과 붙여넣기]
```

## 5. 디렉토리·권한·ACL

| 대상 | 소유자:그룹 | 권한 | 접근 정책 |
|---|---|---:|---|
| `upload_files` | `agent-admin:agent-common` | `2770` | 세 계정 R/W |
| `api_keys` | `agent-admin:agent-core` | `2770` | admin/dev R/W |
| `/var/log/agent-app` | `agent-admin:agent-core` | `2770` | admin/dev R/W |
| `agent-app` | `agent-admin:agent-core` | `750` | 운영 계정 실행 |
| `monitor.sh` | `agent-dev:agent-core` | `750` | dev 소유, admin cron 실행 |

### 확인 명령

```bash
ls -ld /home/agent-admin/agent-app/upload_files
ls -ld /home/agent-admin/agent-app/api_keys
ls -ld /var/log/agent-app
stat -c '%U:%G %a %n' /home/agent-admin/agent-app/agent-app
stat -c '%U:%G %a %n' /home/agent-admin/agent-app/bin/monitor.sh
getfacl /home/agent-admin/agent-app/upload_files
getfacl /home/agent-admin/agent-app/api_keys
getfacl /var/log/agent-app
```

### 실행 결과

```text
[실행 결과 붙여넣기]
```

### 접근 성공·차단 테스트

```bash
sudo -u agent-test touch /home/agent-admin/agent-app/upload_files/test-by-agent-test.txt
sudo -u agent-test ls /home/agent-admin/agent-app/api_keys
sudo -u agent-dev ls -l /home/agent-admin/agent-app/api_keys
```

```text
[실행 결과 붙여넣기]
```

### 최소 권한 설명

`agent-test`는 테스트 파일을 업로드할 필요는 있지만 API 키와 운영 로그를 읽을 필요는 없다. 따라서 공용 작업 영역은 `agent-common`, 민감 정보와 운영 로그는 `agent-core`로 분리했다. `monitor.sh`의 소유자는 개발 역할인 `agent-dev`로 두고 그룹 실행 권한을 부여하여 `agent-core` 구성원인 `agent-admin`이 cron으로 실행할 수 있게 했다. 디렉토리의 setgid와 기본 ACL은 새 파일이 협업 그룹과 권한 정책을 계속 상속하도록 한다.

## 6. 환경 변수와 키 파일

### 실행 환경 변수

```text
AGENT_HOME=/home/agent-admin/agent-app
AGENT_PORT=15034
AGENT_UPLOAD_DIR=/home/agent-admin/agent-app/upload_files
AGENT_KEY_PATH=/home/agent-admin/agent-app/api_keys
AGENT_LOG_DIR=/var/log/agent-app
```

### 키 파일 확인

```bash
ls -l /home/agent-admin/agent-app/api_keys
sudo -u agent-admin cat /home/agent-admin/agent-app/api_keys/t_secret.key
sudo -u agent-admin cat /home/agent-admin/agent-app/api_keys/secret.key
```

```text
[실행 결과 붙여넣기]
```

### 문서와 제공 바이너리의 차이

미션 문서는 `AGENT_KEY_PATH=$AGENT_HOME/api_keys/t_secret.key`와 `t_secret.key`를 요구한다. 그러나 제공 실행 파일의 Boot Sequence를 검증하면 `AGENT_KEY_PATH`가 `/home/agent-admin/agent-app/api_keys` 디렉토리여야 하며 그 안의 `secret.key`를 읽는다. 문서 요구사항과 실제 실행 성공을 모두 보존하기 위해 두 키 파일을 동일한 테스트 값으로 생성하고, 앱 실행에는 바이너리가 요구하는 디렉토리 경로를 사용했다.

환경 변수로 경로와 포트를 고정하면 로그인 셸, cron, 수동 실행처럼 실행 경로가 달라도 같은 구성으로 앱을 시작할 수 있고, 코드 변경 없이 환경별 값을 교체할 수 있다.

## 7. 애플리케이션 Boot Sequence

### 실행 명령

```bash
bash /mission/scripts/start-agent.sh
```

### 성공 결과

```text
[Boot Sequence 5단계 [OK], All Boot Checks Passed!, Agent READY, 포트 출력 붙여넣기]
```

### 프로세스·포트 확인

```bash
bash /mission/scripts/start-agent-background.sh
pgrep -a -u agent-admin -x agent-app
ss -ltnp | grep ':15034'
```

```text
[실행 결과 붙여넣기]
```

## 8. monitor.sh 구현

### 프로세스 식별 명령 선택

```bash
pgrep -o -u agent-admin -x agent-app
```

`pgrep`는 `ps | grep` 조합보다 PID 검색 목적이 명확하고, grep 프로세스 자체가 섞일 위험이 적다. `-u`로 실행 사용자를 제한하고 `-x`로 정확한 프로세스 이름만 선택한다. 제공 앱은 패키징 방식 때문에 PID가 복수로 나타날 수 있어 `-o`로 가장 오래된 대표 PID를 로그에 기록한다.

### 포트 확인 명령 선택

```bash
ss -ltnH
```

프로세스가 존재한다고 해서 서비스 소켓이 정상적으로 열렸다는 뜻은 아니다. `ss`로 실제 TCP LISTEN 소켓을 별도로 확인한다. `netstat`보다 현재 리눅스 환경에서 기본적으로 권장되고 별도 구형 패키지 의존성이 적다.

### CPU/MEM/DISK 추출 방식

- CPU: `/proc/stat`을 1초 간격으로 두 번 읽고 전체 CPU 시간과 idle 시간의 변화량으로 사용률 계산
- MEM: `/proc/meminfo`의 `MemTotal`과 `MemAvailable` 차이를 전체 메모리로 나누어 계산
- DISK: `df -P /`의 루트 파티션 Used `%`를 숫자로 파싱

로그 포맷을 한 줄의 고정된 `KEY:VALUE` 구조로 만든 이유는 사람이 `tail`로 읽기 쉽고, `awk` 같은 표준 도구로 필드를 안정적으로 파싱할 수 있기 때문이다.

### 정상 실행 결과

```bash
sudo -u agent-admin /home/agent-admin/agent-app/bin/monitor.sh
echo $?
```

```text
[실행 결과 붙여넣기]
```

### 경고와 종료 정책

앱 프로세스 또는 포트 비정상은 서비스를 제공할 수 없는 Health Check 실패이므로 `exit 1`로 종료한다. UFW 비활성·상태 확인 실패나 자원 임계값 초과는 즉시 서비스가 중단된 상태는 아니므로 `[WARNING]`을 출력하고 관측값을 로그에 남긴 뒤 정상 종료한다. 일반 계정에서 방화벽 상태만 읽을 수 있도록 `agent-admin`에는 `/usr/sbin/ufw status` 명령에 한정된 NOPASSWD sudo 규칙만 부여하고 방화벽 변경 권한은 주지 않았다. 이 구분으로 cron이나 상위 자동화가 실제 장애와 경고 상태를 서로 다르게 처리할 수 있다.

## 9. monitor.log 누적 기록

### 로그 형식

```text
[YYYY-MM-DD HH:MM:SS] PID:... CPU:..% MEM:..% DISK_USED:..%
```

### 확인 명령

```bash
wc -l /var/log/agent-app/monitor.log
tail -n 5 /var/log/agent-app/monitor.log
```

### 실행 결과

```text
[실행 결과 붙여넣기]
```

### `>`와 `>>` 차이

`>`는 대상 파일을 새로 열면서 기존 내용을 덮어쓴다. `>>`는 기존 파일의 끝에 데이터를 추가한다. 모니터링 로그는 시간 순서에 따른 상태 변화와 장애 직전 데이터를 보존해야 하므로 `>>`로 누적해야 한다.

## 10. cron 자동 실행

### 등록 내용

```cron
* * * * * /home/agent-admin/agent-app/bin/monitor.sh >> /var/log/agent-app/cron.log 2>&1
```

### 등록·자동 증가 확인

```bash
sudo -u agent-admin crontab -l
before=$(wc -l < /var/log/agent-app/monitor.log)
sleep 70
after=$(wc -l < /var/log/agent-app/monitor.log)
echo "before=$before after=$after"
tail -n 5 /var/log/agent-app/monitor.log
```

```text
[실행 결과 붙여넣기]
```

## 11. 비정상 상태 exit 1

### 테스트

```bash
bash /mission/scripts/stop-agent.sh
bash /mission/scripts/test-monitor-failure.sh
bash /mission/scripts/start-agent-background.sh
```

### 실행 결과

```text
[monitor.sh가 프로세스 비정상을 출력하고 exit 1을 반환한 결과 붙여넣기]
```

## 12. 로그 용량 관리

### logrotate 설정

```text
size 10M
rotate 10
compress
delaycompress
copytruncate
create 0660 agent-admin agent-core
su agent-admin agent-core
```

### 설정·동작 확인

```bash
cat /etc/logrotate.d/agent-app
logrotate -d /etc/logrotate.d/agent-app
dd if=/dev/zero bs=1M count=11 >> /var/log/agent-app/monitor.log
logrotate -f /etc/logrotate.d/agent-app
ls -lh /var/log/agent-app
```

```text
[실행 결과 붙여넣기]
```

### 구현 방식 설명

`size 10M`은 파일이 10MB 이상일 때 회전시키고, `rotate 10`은 이전 파일을 최대 10개 유지한다. `compress`는 오래된 로그의 디스크 사용량을 줄인다. `copytruncate`는 현재 파일을 복사한 뒤 원본을 비워, 로그를 계속 여는 프로세스나 반복 실행 스크립트가 같은 경로에 기록할 수 있게 한다. `/etc/cron.d/agent-logrotate`가 5분마다 크기 조건을 확인한다.

## 13. 전체 검증

```bash
bash /mission/scripts/verify.sh
```

```text
[PASS/FAIL 요약 붙여넣기]
```

## 14. 운영 상황 설명

### 모니터링 대상이 Nginx로 변경되는 경우

핵심적으로 바꿀 항목은 프로세스 이름, 서비스 사용자, 포트, 로그 경로, 임계값이다. Nginx라면 프로세스를 `nginx`, 포트를 80 또는 443으로 변경하고, 애플리케이션 로그 대신 access/error 로그와 HTTP 응답 확인을 추가할 수 있다. 단순 LISTEN 확인 외에 `curl`을 이용한 실제 응답 검증을 추가하면 Health Check 품질이 높아진다.

### 프로세스는 살아 있지만 포트가 열리지 않는 경우

가능한 원인은 초기화 실패, 설정 오류, 포트 충돌, 다른 주소·포트 바인딩, 권한 문제, 내부 서버 스레드 실패다. 확인 순서는 다음과 같다.

```bash
pgrep -a -u agent-admin -x agent-app
ss -ltnp | grep ':15034'
tail -n 100 /var/log/agent-app/agent-app.log
env | grep '^AGENT_'
```

프로세스 상태, 실제 소켓, 앱 로그, 실행 환경 변수를 순서대로 비교하여 원인을 좁힌다.

### 로그 급증과 디스크 고갈 위험 대응

단기적으로 `df -h`, `df -i`, `du`로 용량과 inode, 큰 파일을 확인하고 logrotate를 실행하거나 안전하게 오래된 로그를 압축·이동한다. 무조건 파일을 삭제하기 전에 장애 분석에 필요한 구간을 보존해야 한다. 중장기적으로 보존 기간과 회전 개수를 조정하고, 임계값 경고, 중앙 로그 저장소 전송, 애플리케이션 로그 레벨 조정을 적용한다.

```bash
df -h /
df -i /
du -ah /var/log | sort -h | tail
```

## 15. 보너스: report.sh

```bash
sudo -u agent-admin /home/agent-admin/agent-app/bin/report.sh
```

```text
[선택 수행 결과 붙여넣기]
```

## 16. 최종 체크리스트

- [ ] SSH 포트 20022 설정
- [ ] Root 원격 로그인 차단
- [ ] UFW 활성화 및 20022/15034만 허용
- [ ] 계정·그룹 구성
- [ ] 디렉토리·소유권·ACL 구성
- [ ] Boot Sequence 5단계 `[OK]`
- [ ] `Agent READY`
- [ ] 15034 LISTEN
- [ ] monitor.sh 정상 실행 및 자원 수집
- [ ] 프로세스·포트 비정상에서 `exit 1`
- [ ] 지정 형식 monitor.log 누적
- [ ] cron 매분 실행 및 로그 증가
- [ ] logrotate 10MB/10개 설정 및 동작
- [ ] 설명형 평가 질문 답변 준비
